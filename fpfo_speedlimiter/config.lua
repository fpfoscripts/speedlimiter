config = {}

config.globallimiter = true  -- global speed limit for every vehicle?
config.modellimiter = false  -- only speed limit for vehicles below?
config.speedlimit = 120      -- what is the speed limit?
config.usemph = false        -- true = mph, false = kmh

-- Models to be limited (if config.modellimiter = true)
config.models = {
    --"adder",
    --"zentorno",
    --"t20"
}